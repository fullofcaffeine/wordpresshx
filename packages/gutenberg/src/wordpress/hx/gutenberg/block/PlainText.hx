package wordpress.hx.gutenberg.block;

/**
 * Exact `PlainText` export from `@wordpress/block-editor` 15.13.0.
 *
 * `@:genes.jsxComponentProps` connects this runtime class value to the closed
 * Haxe property contract checked for `<PlainText ... />`. Genes consumes the
 * metadata while planning JSX and emits only the native named import—no wrapper
 * or runtime validator.
 */
@:genes.jsxComponentProps("wordpress.hx.gutenberg.block.PlainTextProps")
@:jsRequire("@wordpress/block-editor", "PlainText")
extern class PlainText {}
